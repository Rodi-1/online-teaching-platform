# Быстрый старт - Schedule Service

## Запуск через Docker Compose (рекомендуется)

Из корня проекта:

```bash
# Запустить все сервисы (включая schedule-service)
docker-compose up -d

# Или запустить только schedule-service
docker-compose up -d schedule-service

# Проверить статус
docker-compose ps

# Посмотреть логи
docker-compose logs -f schedule-service
```

## Доступ к API

- **API**: http://localhost:8007
- **Swagger UI**: http://localhost:8007/docs
- **ReDoc**: http://localhost:8007/redoc
- **Health check**: http://localhost:8007/health

## Локальный запуск (разработка)

```bash
cd services/schedule-service

# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Активировать (Linux/Mac)
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Настроить .env (создать из примера)
# Указать подключение к БД

# Запустить сервис
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

## Тестирование API

Все запросы требуют заголовки для авторизации:
- `x-user-id`: UUID пользователя
- `x-user-role`: роль (teacher, student, admin)

### 1. Создать занятие (преподаватель)

```bash
curl -X POST http://localhost:8007/api/courses/123e4567-e89b-12d3-a456-426614174000/lessons \
  -H "x-user-id: 12345678-1234-5678-1234-567812345678" \
  -H "x-user-role: teacher" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Функции. Введение",
    "description": "Обсуждаем базовые определения и примеры функций",
    "start_at": "2025-02-21T16:00:00Z",
    "end_at": "2025-02-21T17:00:00Z",
    "location_type": "online",
    "online_link": "https://meet.example.com/lesson1",
    "room": null
  }'
```

### 2. Получить информацию о занятии

```bash
curl -X GET http://localhost:8007/api/lessons/{lesson_id}
```

### 3. Изменить занятие (преподаватель)

```bash
curl -X PATCH http://localhost:8007/api/lessons/{lesson_id} \
  -H "x-user-id: 12345678-1234-5678-1234-567812345678" \
  -H "x-user-role: teacher" \
  -H "Content-Type: application/json" \
  -d '{
    "start_at": "2025-02-21T17:00:00Z",
    "end_at": "2025-02-21T18:00:00Z",
    "location_type": "offline",
    "room": "Кабинет 302",
    "online_link": null
  }'
```

### 4. Получить расписание пользователя

```bash
curl -X GET "http://localhost:8007/api/schedule/me?offset=0&count=50" \
  -H "x-user-id: 87654321-4321-8765-4321-876543218765" \
  -H "x-user-role: student"

# С фильтрацией по датам
curl -X GET "http://localhost:8007/api/schedule/me?date_from=2025-02-01T00:00:00Z&date_to=2025-02-28T23:59:59Z" \
  -H "x-user-id: 87654321-4321-8765-4321-876543218765" \
  -H "x-user-role: student"
```

### 5. Получить расписание курса

```bash
curl -X GET http://localhost:8007/api/courses/123e4567-e89b-12d3-a456-426614174000/schedule

# С фильтрацией
curl -X GET "http://localhost:8007/api/courses/123e4567-e89b-12d3-a456-426614174000/schedule?date_from=2025-02-01T00:00:00Z&date_to=2025-02-28T23:59:59Z"
```

### 6. Отметить посещаемость (преподаватель)

```bash
curl -X POST http://localhost:8007/api/lessons/{lesson_id}/attendance \
  -H "x-user-id: 12345678-1234-5678-1234-567812345678" \
  -H "x-user-role: teacher" \
  -H "Content-Type: application/json" \
  -d '{
    "items": [
      {
        "student_id": "87654321-4321-8765-4321-876543218765",
        "status": "present",
        "comment": null
      },
      {
        "student_id": "99999999-9999-9999-9999-999999999999",
        "status": "absent",
        "comment": "Болел"
      }
    ]
  }'
```

### 7. Получить посещаемость

```bash
# Преподаватель видит всех
curl -X GET http://localhost:8007/api/lessons/{lesson_id}/attendance \
  -H "x-user-id: 12345678-1234-5678-1234-567812345678" \
  -H "x-user-role: teacher"

# Студент видит только себя
curl -X GET http://localhost:8007/api/lessons/{lesson_id}/attendance \
  -H "x-user-id: 87654321-4321-8765-4321-876543218765" \
  -H "x-user-role: student"
```

## Запуск тестов

```bash
cd services/schedule-service

# Установить зависимости (если еще не установлены)
pip install -r requirements.txt

# Запустить тесты
pytest

# С подробным выводом
pytest -v

# С покрытием
pytest --cov=app
```

## Переменные окружения

Основные переменные (настраиваются в `.env` корня проекта):

```env
# Database
SCHEDULE_SERVICE_DB_USER=schedule_service
SCHEDULE_SERVICE_DB_PASSWORD=your_password
SCHEDULE_SERVICE_DB_NAME=schedule_service_db

# Environment
ENV=local
LOG_LEVEL=INFO
```

## Примеры использования

### Типичный сценарий: Создание занятия

1. Преподаватель создает занятие через `POST /api/courses/{id}/lessons`
2. Система валидирует даты (end_at > start_at)
3. Занятие сохраняется со статусом "scheduled"
4. Студенты видят занятие в своем расписании через `GET /api/schedule/me`

### Типичный сценарий: Отметка посещаемости

1. После занятия преподаватель вызывает `POST /api/lessons/{id}/attendance`
2. Система создает или обновляет записи посещаемости
3. Преподаватели могут просмотреть полную посещаемость
4. Студенты могут проверить свою посещаемость

## Валидация и ограничения

### Создание занятий
- ✅ Только teacher и admin
- ✅ end_at должно быть больше start_at
- ✅ location_type: "online" или "offline"
- ✅ Обязательные поля: title, start_at, end_at, location_type

### Обновление занятий
- ✅ Только teacher и admin
- ✅ Нельзя обновлять занятия со статусом "finished"
- ✅ Валидация дат при изменении

### Посещаемость
- ✅ Отметить могут только teacher и admin
- ✅ status: "present", "absent", или "late"
- ✅ Одна запись на студента
- ✅ Преподаватели видят всё, студенты - только себя

## Troubleshooting

### Порт занят

Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8008:8000"  # Вместо 8007
```

### База данных не инициализировалась

```bash
# Пересоздать контейнеры
docker-compose down -v
docker-compose up -d
```

### Ошибка прав доступа (403)

Убедитесь, что:
1. Заголовок `x-user-role` установлен правильно
2. Роль соответствует требуемой (teacher/admin для создания)
3. Заголовки передаются в запросе

### Ошибка валидации дат (400)

Проверьте, что:
1. end_at > start_at
2. Даты в формате ISO 8601 (UTC)
3. Формат: "YYYY-MM-DDTHH:MM:SSZ"

## Swagger UI

Самый простой способ тестировать API:

1. Откройте http://localhost:8007/docs
2. Нажмите "Authorize" (если требуется)
3. Добавьте заголовки в "Headers" секции
4. Тестируйте endpoints прямо в браузере

## Полезные команды

```bash
# Посмотреть логи в реальном времени
docker-compose logs -f schedule-service

# Перезапустить сервис
docker-compose restart schedule-service

# Остановить сервис
docker-compose stop schedule-service

# Удалить сервис и данные
docker-compose down -v schedule-service

# Войти в контейнер
docker exec -it schedule-service bash

# Проверить подключение к БД из контейнера
docker exec -it schedule-service python -c "from app.db.session import engine; print(engine.connect())"
```

## Следующие шаги

1. Изучите API через Swagger UI: http://localhost:8007/docs
2. Протестируйте все endpoints
3. Посмотрите примеры в тестах: `tests/test_api_schedule.py`
4. Изучите код в `app/` директории
5. Интегрируйте с Course Service для реальных данных

## Полезные ссылки

- [README.md](README.md) - Полная документация
- [IMPLEMENTATION_SUMMARY.md](IMPLEMENTATION_SUMMARY.md) - Детальный отчет
- [Основной README](../../README.md) - Документация проекта

Удачи! 🚀

