# 🔧 Рефакторинг: Удаление неиспользуемой переменной `instrumentator`

## Проблема

После изменения функции `setup_metrics()` на возврат `None` (вместо `Instrumentator`), в 7 из 8 микросервисов оставалась неиспользуемая переменная `instrumentator`.

### Было:

```python
instrumentator = setup_metrics(
    app=app,
    service_name="homework-service",
    service_version=settings.APP_VERSION
)
```

**Проблемы:**
- ❌ Переменная `instrumentator` не используется нигде далее в коде
- ❌ `setup_metrics()` теперь возвращает `None`
- ❌ Создаётся мёртвый код (dead code)
- ❌ Несогласованность: `user-service` был исправлен, остальные - нет

## Решение

Убрали присваивание во всех сервисах:

```python
setup_metrics(
    app=app,
    service_name="homework-service",
    service_version=settings.APP_VERSION
)
```

## Исправленные файлы

✅ `services/homework-service/app/main.py` (строка 55)  
✅ `services/gradebook-service/app/main.py` (строка 39)  
✅ `services/profile-service/app/main.py` (строка 63)  
✅ `services/notifications-service/app/main.py` (строка 63)  
✅ `services/tests-service/app/main.py` (строка 57)  
✅ `services/schedule-service/app/main.py` (строка 55)  
✅ `services/reports-service/app/main.py` (строка 55)  

## Текущий статус

✅ **Все 8 микросервисов** теперь согласованы  
✅ **Нет мёртвого кода**  
✅ **Нет linter warnings**  
✅ **Функциональность не изменилась** - метрики работают корректно  

## Контекст изменения

Эта правка является частью рефакторинга системы метрик, где:
1. `setup_metrics()` теперь самостоятельно регистрирует endpoint `/metrics`
2. Возвращаемое значение больше не требуется
3. Вся конфигурация инкапсулирована внутри функции

## Проверка

```powershell
# Перезапуск контейнеров (опционально)
docker compose up -d --build

# Проверка что метрики работают
@(8001..8008) | ForEach-Object {
    Write-Host "Port $_: " -NoNewline
    curl "http://localhost:$_/metrics" -UseBasicParsing | Select-String "http_requests_total" | Measure-Object | Select-Object -ExpandProperty Count
}
```

**Ожидаемый результат:** Каждый порт возвращает метрики (Count > 0)

---

**Дата:** 2025-12-07  
**Тип:** Code cleanup / Refactoring  
**Риск:** Минимальный (только удаление неиспользуемой переменной)

