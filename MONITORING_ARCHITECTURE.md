# 🏗️ Архитектура Мониторинга и Логирования

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         ВАША ПЛАТФОРМА                                  │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────┐
│                         🔵 МИКРОСЕРВИСЫ (8 шт)                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │  user    │ │ homework │ │gradebook │ │ profile  │ │  tests   │      │
│  │ :8001    │ │  :8002   │ │  :8003   │ │  :8004   │ │  :8006   │      │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘      │
│       │            │            │            │            │             │
│  ┌────┴────┐  ┌────┴────┐  ┌────┴────┐                                 │
│  │schedule │  │ reports │  │ notify  │                                 │
│  │  :8007  │  │  :8008  │  │  :8005  │                                 │
│  └────┬────┘  └────┬────┘  └────┬────┘                                 │
│       │            │            │                                       │
│       └────────────┴────────────┘                                       │
│                    │                                                     │
│         📝 Генерируют ЛОГИ (JSON)                                       │
│         📊 Отдают МЕТРИКИ (/metrics)                                    │
└──────────────────────┬──────────────────────────────────────────────────┘
                       │
          ┌────────────┴────────────┐
          │                         │
          ▼                         ▼
┌─────────────────┐       ┌─────────────────┐
│  📝 ЛОГИ        │       │  📊 МЕТРИКИ     │
└─────────────────┘       └─────────────────┘


═══════════════════════════════════════════════════════════════════════════
                          📝 ПОТОК ЛОГОВ
═══════════════════════════════════════════════════════════════════════════

┌────────────────────────────────────────────────────────────────┐
│  Микросервисы → пишут логи в stdout/stderr (Docker)            │
└──────────────────────┬─────────────────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────┐
        │   🔸 PROMTAIL :9080      │  ← Собирает логи из Docker
        │   (Log Collector)        │     /var/lib/docker/containers/
        └─────────────┬────────────┘
                      │
                      │ отправляет HTTP
                      ▼
        ┌──────────────────────────┐
        │   🔹 LOKI :3100          │  ← Хранит и индексирует
        │   (Log Aggregation)      │     логи (как БД для логов)
        └─────────────┬────────────┘
                      │
                      │ запросы LogQL
                      ▼
        ┌──────────────────────────┐
        │   📊 GRAFANA :3000       │  ← Визуализация логов
        │   (Visualization)        │     
        └──────────────────────────┘


Что храним в Loki:
├── timestamp         ⏰ Время события
├── level            📝 INFO/ERROR/WARNING
├── message          💬 Текст лога
├── service          🔖 Имя сервиса
├── request_id       🆔 ID запроса (для трейсинга)
├── duration_ms      ⏱️ Время выполнения
└── метаданные       📋 container_name, host, etc.


═══════════════════════════════════════════════════════════════════════════
                         📊 ПОТОК МЕТРИК
═══════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│  Микросервисы → экспортируют метрики на /metrics endpoint       │
│  (Prometheus FastAPI Instrumentator)                            │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     │
        ┌────────────┴──────────────┐
        │                           │
        ▼                           ▼
┌───────────────────┐      ┌────────────────────┐
│ user-service      │      │  PostgreSQL        │
│ :8001/metrics     │      │  :5432             │
│ ...               │      └──────────┬─────────┘
│ (8 сервисов)      │                 │
└───────┬───────────┘                 │
        │                             ▼
        │              ┌─────────────────────────┐
        │              │ postgres-exporter       │
        │              │ :9187/metrics           │
        │              └──────────┬──────────────┘
        │                         │
        │    HTTP GET /metrics    │
        └─────────────┬───────────┘
                      │
                      ▼
        ┌──────────────────────────┐
        │  📈 PROMETHEUS :9090     │  ← Собирает (scrape) и
        │  (Metrics Database)      │     хранит метрики
        └─────────────┬────────────┘     каждые 15 секунд
                      │
                      │ PromQL запросы
                      ▼
        ┌──────────────────────────┐
        │   📊 GRAFANA :3000       │  ← Визуализация метрик
        │   (Visualization)        │     
        └──────────────────────────┘


Какие метрики собираем:

📊 HTTP Метрики (FastAPI):
  ├── http_requests_total           - общее кол-во запросов
  ├── http_request_duration_seconds - время ответа (histogram)
  ├── http_requests_in_progress     - активные запросы
  └── по labels: service, method, path, status_code

📊 PostgreSQL Метрики (postgres-exporter):
  ├── pg_stat_database_numbackends  - активные подключения
  ├── pg_stat_database_tup_fetched  - прочитано строк
  ├── pg_stat_database_tup_inserted - вставлено строк
  ├── pg_stat_database_conflicts    - конфликты
  └── pg_database_size_bytes        - размер БД


═══════════════════════════════════════════════════════════════════════════
                    🎨 GRAFANA - Центр визуализации
═══════════════════════════════════════════════════════════════════════════

                    ┌─────────────────────────┐
                    │   GRAFANA :3000         │
                    │   admin / admin         │
                    └──────────┬──────────────┘
                               │
              ┌────────────────┴─────────────────┐
              │                                  │
              ▼                                  ▼
    ┌──────────────────┐              ┌──────────────────┐
    │  Data Source:    │              │  Data Source:    │
    │  Prometheus      │              │  Loki            │
    │  (Metrics)       │              │  (Logs)          │
    └────────┬─────────┘              └────────┬─────────┘
             │                                 │
             ▼                                 ▼
    ┌──────────────────┐              ┌──────────────────┐
    │  📊 Dashboards:  │              │  📝 Dashboards:  │
    │                  │              │                  │
    │  • System        │              │  • Logs          │
    │    Overview      │              │    Explorer      │
    │  • Service       │              │  • Error         │
    │    Detail        │              │    Tracking      │
    │  • PostgreSQL    │              │  • Request       │
    │  • Alerts        │              │    Tracing       │
    └──────────────────┘              └──────────────────┘


═══════════════════════════════════════════════════════════════════════════
                         🔄 ПОЛНАЯ СХЕМА
═══════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────┐
│                        USER REQUEST                                 │
│                             ↓                                       │
│                   [Microservice :8001]                              │
│                      ↓            ↓                                 │
│                  📝 Logs      📊 Metrics                            │
│                      ↓            ↓                                 │
│              ┌──────────┐   ┌──────────┐                           │
│              │ Promtail │   │Prometheus│                           │
│              │  :9080   │   │  :9090   │                           │
│              └────┬─────┘   └────┬─────┘                           │
│                   │              │                                  │
│                   ▼              │                                  │
│              ┌──────────┐        │                                  │
│              │   Loki   │        │                                  │
│              │  :3100   │        │                                  │
│              └────┬─────┘        │                                  │
│                   │              │                                  │
│                   └──────┬───────┘                                  │
│                          ▼                                          │
│                    ┌───────────┐                                    │
│                    │  Grafana  │ ← YOU ARE HERE                     │
│                    │   :3000   │   (Dashboard UI)                   │
│                    └───────────┘                                    │
└─────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════
                    📝 ЧТО ВИДНО В GRAFANA
═══════════════════════════════════════════════════════════════════════════

ЛОГИ (из Loki):
✅ {"timestamp": "2025-12-07T01:06:23Z", "level": "INFO", 
    "service": "user-service", "message": "Request completed",
    "request_id": "abc-123", "duration_ms": 45, "status_code": 200}

✅ Можно искать по:
   - service name
   - уровню (ERROR/INFO/WARNING)
   - request_id (трейсинг)
   - тексту ошибки
   - времени выполнения

МЕТРИКИ (из Prometheus):
✅ http_requests_total{service="user-service", status_code="200"} = 1523
✅ http_request_duration_seconds{quantile="0.95"} = 0.124
✅ pg_stat_database_numbackends = 12

✅ Можно строить графики:
   - RPS (requests per second)
   - Response time (p50, p95, p99)
   - Error rate %
   - Database connections
   - Custom бизнес-метрики


═══════════════════════════════════════════════════════════════════════════
                    🚀 БЫСТРЫЙ СТАРТ
═══════════════════════════════════════════════════════════════════════════

1. Откройте: http://localhost:3000 (admin/admin)

2. Добавьте Data Sources:
   • Prometheus → http://prometheus:9090
   • Loki → http://loki:3100

3. Импортируйте dashboards:
   • ID 11133 (FastAPI)
   • ID 13639 (Loki)
   • ID 9628 (PostgreSQL)

4. Или используйте готовый: grafana-dashboard-metrics.json

5. Explore → Loki → {container_name=~".*-service"}
   Увидите все логи!

═══════════════════════════════════════════════════════════════════════════
```

